<?xml version="1.0" encoding="utf-8"?>
<manifest package="cav.sanyd"
          xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:name=".SanydApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".ui.activitys.MainActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.activitys.AboutActivity"
            android:label="@string/about_label"
            android:theme="@style/AppTheme.NoActionBar">
        </activity>
        <activity
            android:name=".ui.activitys.StatisticActivity"
            android:label="@string/statistiv_label"
            android:theme="@style/AppTheme.NoActionBar">
        </activity>
        <activity android:name=".ui.activitys.PlanTodayActivity"
            android:label="@string/plantoday_label"
                  android:theme="@style/AppTheme.NoActionBar">
        </activity>
        <activity android:name=".ui.activitys.PlanTomorrowActivity"
                  android:theme="@style/AppTheme.NoActionBar"
            android:label="@string/plantomorrow_label">
        </activity>
    </application>

</manifest>